import React from 'react';
import styled from 'styled-components';

const MainDiv = styled.main`
    
`

const Main = () => {
    return (
        <MainDiv>
            
        </MainDiv>
    );
};

export default Main;